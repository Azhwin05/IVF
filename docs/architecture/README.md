# Architecture Docs

The primary architecture assessment lives at [`/ARCHITECTURE.md`](../../ARCHITECTURE.md) (current-state audit, target architecture, database model, risk assessment). This folder holds detailed design notes as individual modules are built out in later phases — one file per module once its schema/API/event design is finalized (e.g. `patients.md`, `billing.md`, `pharmacy.md`).

Nothing to add here yet — Phase 0 is audit-only. Populate per-module design docs starting in Phase 2.
